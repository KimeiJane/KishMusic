{
  "name": "kishmusic",
  "version": "1.0.0",
  "description": "A single-page music application",
  "scripts": {
    "start": "npx json-server --watch db.json --port 3000",
    "dev": "npx json-server --watch db.json --port 3000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "json-server": "^0.17.4"
  },
  "main": "app.js",
  "keywords": [],
  "author": "",
  "license": "ISC"
}
